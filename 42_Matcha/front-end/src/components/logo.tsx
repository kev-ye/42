import {SvgIcon} from "@mui/material";
import {Si42} from "react-icons/si";
import * as React from "react";

export const FortyTwoIcon = (props: any) => {
  return (
    <SvgIcon {...props}>
      <Si42 />
    </SvgIcon>
  )
}